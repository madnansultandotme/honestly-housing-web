# Curated Option Card

Build a card for a curated option (Good/Better/Best). Include fields: tier label, item name, image, price, upgrade difference. Apply selection state styling to highlight the chosen option.
