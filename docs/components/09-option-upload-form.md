# Option Upload Form

Build a form to add a curated option with title, linkUrl, image, price, and category. Implement image upload to Firebase Storage and save the URL. Create a Firestore write to options collection with the form data.
